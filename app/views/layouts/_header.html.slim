.wrapper
  .logo
    = link_to(image_tag("babyhouse.png", :alt => 'Baby House'), "/")
  nav#main_menu
    ul
      li
        = link_to("Sobre a Baby House", "#", :class => "menu1")
        ul.submenu
          .arrow
          - for page in @about_pages
            li= link_to page.title, page_path(page)
      li
        = link_to("Pedagogia para a Vida", "#", :class => "menu1")
        - if @pedagogy_pages
          ul.submenu
            .arrow
            - for page in @pedagogy_pages
              li= link_to page.title, page_path(page)
      li
        = link_to("Portal das famílias", "#{parent_signed_in? ? '#' : new_parent_session_path}", :class => "menu1")
        ul.submenu
          .arrow
          - if parent_signed_in?
            li = link_to "Sair", destroy_parent_session_path, method: :delete
          - else
            li = link_to "Fazer login", new_parent_session_path
        - if @portal_pages
          - for page in @portal_pages
            li= link_to page.title, page_path(page)

      li
        = link_to("Entre em contato", contato_path, :class => "menu1")
  .clear
